/** Align with RainViewer scan cadence (~10 min new past scans). */
export const RADAR_FRAMES_POLL_MS = 10 * 60 * 1000;

/** MapLibre raster crossfade when tile path changes. */
export const RADAR_RASTER_FADE_MS = 1100;
